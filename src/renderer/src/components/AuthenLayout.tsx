import React from 'react';

export const AuthenLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            AuthenLayout
            {children}
        </div>
    );
};
